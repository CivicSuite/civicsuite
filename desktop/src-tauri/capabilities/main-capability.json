{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "CivicSuite local desktop: city-work commands scoped to the local main window. Per-command allow-* identifiers are not generated by this generate_handler! configuration, so command-surface scoping relies on windows:[main] + local:true (no remote origin can invoke).",
  "windows": ["main"],
  "local": true,
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:event:default"
  ]
}
