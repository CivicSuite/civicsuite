"""Alembic migration versions for civiccore.

Alembic discovers migration files by scanning this directory — the
__init__.py is present only so the test suite can import version-module
constants (e.g. _SHARED_TABLE_ORDER from the baseline migration) via
standard Python package semantics.
"""
