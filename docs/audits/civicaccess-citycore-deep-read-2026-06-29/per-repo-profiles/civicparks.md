# civicparks — repo profile (2026-06-29 deep-read)
_Generated by the 35-agent CivicSuite-org deep-read workflow (run wf_f2142395-937)._

**One-line purpose**:

Parks and recreation support foundation for CivicSuite: cited parks policy Q&A, program and facility Q&A, registration assistance that links to existing systems, maintenance request triage for Civic311-style handoff, with optional database-backed registration/maintenance workpapers.

**Code languages**:

- Python (FastAPI)

**Pinned versions**:

- **civiccore_required**: 0.3.0
- **current_version**: 0.1.1

**Entrypoints**:

- path: C:\dev\civicsuite-org-readall\civicparks\civicparks\main.py | kind: FastAPI app | purpose: Primary entrypoint; spawns FastAPI app instance for policy Q&A, program Q&A, registration assistance, and maintenance triage endpoints. Requires: `python -m uvicorn civicparks.main:app --host 127.0.0.1 --port 8143`
- path: C:\dev\civicsuite-org-readall\civicparks\civicparks\__init__.py | kind: Package version | purpose: Exports __version__ = 0.1.1

**Public HTTP routes**:

- method: GET | path: / | auth: none | persistence: none
- method: GET | path: /health | auth: none | persistence: none
- method: GET | path: /civicparks | auth: none | persistence: HTML landing page
- method: POST | path: /api/v1/civicparks/policy-answer | auth: none | persistence: none - deterministic answer
- method: POST | path: /api/v1/civicparks/program-answer | auth: none | persistence: none - deterministic answer
- method: POST | path: /api/v1/civicparks/registration-assistance | auth: none | persistence: optional SQLAlchemy to CIVICPARKS_WORKPAPER_DB_URL
- method: GET | path: /api/v1/civicparks/registration-assistance/{assistance_id} | auth: none | persistence: reads from CIVICPARKS_WORKPAPER_DB_URL; 503 if unconfigured, 404 if missing
- method: POST | path: /api/v1/civicparks/maintenance-triage | auth: none | persistence: optional SQLAlchemy to CIVICPARKS_WORKPAPER_DB_URL
- method: GET | path: /api/v1/civicparks/maintenance-triage/{triage_id} | auth: none | persistence: reads from CIVICPARKS_WORKPAPER_DB_URL; 503 if unconfigured, 404 if missing

**Tests overview**:

- **count**: 3
- **frameworks**: ['pytest', 'fastapi.testclient']
- **what_they_assert**: Test suite (test_civicparks_foundation.py, test_runtime_foundation.py, test_production_depth_parks_persistence.py) verifies: (1) policy Q&A returns cited answers with staff_review_required=True; (2) program Q&A preserves accessibility_review_required and registration_system_required flags; (3) registration assistance links out without enrolling; (4) maintenance triage categorizes by keyword and returns civic311_handoff_ready=True without dispatching; (5) FastAPI root/health/public-ui endpoints report honest state boundaries; (6) CivicCore 0.3.0 pinning; (7) optional SQLAlchemy persistence to external DB via CIVICPARKS_WORKPAPER_DB_URL with full round-trip CRUD; (8) actionable 503/404 error responses when persistence misconfigured.

**Most recent CHANGELOG entry**:

## [0.1.1] - 2026-04-28\n\n### Added\n\n- Optional SQLAlchemy-backed registration assistance and maintenance triage workpaper records via `CIVICPARKS_WORKPAPER_DB_URL`.\n- Registration assistance and maintenance triage retrieval endpoints for persisted records.\n\n### Changed\n\n- Dependency-alignment release: moved CivicParks to `civiccore==0.3.0` while preserving the existing v0.1.0 runtime foundation behavior.\n- Updated CI, verification gates, package metadata, docs, runtime tests, landing page, and public UI labels for the v0.1.1 release.

---

**How the shipping desktop consumes this repo**:

(D) Registry declaration only, not actively wired into desktop UI or supervisor. Evidence: (1) civicparks appears in installer/modules.json as selectable tier-3 module with civiccore_requirement 0.3.0, default_port 8078, installer_status 'tracked_suite_module_needs_installer_proof', and proof_required=[install, health_check, restart]. (2) No routes, services, permissions, migrations, tasks, lifecycle, backup_restore_hooks, or model_needs fields present in modules.json entry — these are REQUIRED by module_registry.rs validate_installable_module_contract (line 268-310) for any module targeting Windows Local 1.0, hence civicparks cannot pass contract validation and is not installable per current contracts. (3) No references to civicparks in desktop/src, desktop/src-tauri/src (workflows.rs, supervisor.rs, main.rs, etc.), or generated runtime code. (4) Not bundled in city-core profile (which includes civiccore, civicrecords-ai, civicclerk, civiccode, civicnotice, civicaccess). Conclusion: civicparks is tracked in the registry and available for manual custom selection, but lacks the full Module 1.0 contract signature (routes, services, lifecycle, etc.) required for Windows Local desktop integration.

**Module-6 relevance**:

CivicParks is a peer module to CivicAccess (both tier-3 selectable workflow modules) and follows the same incomplete-contract pattern. Both are registered in modules.json but lack the full Module 1.0 contract (routes, services, permissions, migrations, tasks, lifecycle, backup_restore_hooks, model_needs) required for Windows Local 1.0 install validation. For Phase B/C/D CivicAccess integration: (1) Define routes in modules.json for CivicAccess entry points (e.g., /access/intake, /access/status); (2) Declare services with health_check endpoints; (3) Add lifecycle handlers for install/update/disable/uninstall; (4) Define permissions if CivicAccess gates features; (5) Add migrations if data model is versioned; (6) Declare any backup_restore_hooks for state preservation. See civiccode and civicnotice in modules.json (C:\dev\Codex\civicsuite\installer\modules.json) for examples of fully-compliant contracts with routes, services, lifecycle, and hooks. CivicParks itself provides no integration patterns (its desktop wiring is zero) but its module registry structure and optional-DB persistence pattern (envvar configuration + SQLAlchemy schema handling) inform how to wire CivicAccess at scale.

**Notable findings**:

- CivicParks is explicitly boundary-marked as NOT implementing: payments, registrations, participant records, reservation writes, crew dispatch, live LLM calls, or system-of-record replacement (see public_ui.py, README, architecture.md, every endpoint boundary field). Deterministic-only design (no live LLM runtime, no connectors) makes it read-only for policy/program answers — write-path is registration linkout and maintenance triage triage only, with optional workpaper persistence to external DB.
- Module contract gap: civicparks cannot install on Windows Local 1.0 because it lacks routes, services, permissions, migrations, tasks, lifecycle, and model_needs declarations in modules.json. The desktop module_registry.rs enforces this validation at install-time (validate_installable_module_contract), so civicparks registration in the registry is inert until those contract fields are added.
- SQLAlchemy persistence is optional and DB-agnostic: code handles both SQLite (in-memory default) and server databases (PostgreSQL, MySQL via create_engine dialect). Tables are schema-scoped to 'civiccore' namespace with SQLite schema translation to None (no physical schema in SQLite).

